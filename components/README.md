# Components

This directory contains all React components for the ShareView Platform.

## Structure

- `auth/` - Authentication-related components
- `dashboard/` - Dashboard and analytics components
- `client/` - Client-facing components
- `shared/` - Shared/common components used across the application
