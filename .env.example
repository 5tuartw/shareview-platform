# Database Configuration
# Local development with Cloud SQL Proxy
DATABASE_URL=postgresql://user:password@127.0.0.1:5436/database_name

# Production: Use Cloud SQL connection name
# DATABASE_URL=postgresql://user:password@/database_name?host=/cloudsql/project:region:instance

# NextAuth.js Configuration
# Generate secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here
NEXTAUTH_URL=http://localhost:3000

# Migration Configuration
MIGRATION_AUTO_RUN=false  # Set to true to auto-run migrations on startup
INITIAL_ADMIN_EMAIL=admin@example.com
INITIAL_ADMIN_PASSWORD=change-this-password  # Change in production

# Optional: Cloud SQL (production)
# CLOUD_SQL_CONNECTION_NAME=project:region:instance

# Optional: Node Environment
# NODE_ENV=development
