# ShareView Database (Primary)
# For local development with Cloud SQL Proxy on port 5437:
SV_DATABASE_URL=postgresql://sv_user:ShareView2026!@127.0.0.1:5437/shareview
DATABASE_URL=postgresql://sv_user:ShareView2026!@127.0.0.1:5437/shareview

# RSR Analytics Database (for sync operations)
# For local development with Cloud SQL Proxy on port 5436:
RSR_DATABASE_URL=postgresql://analytics_user:AnalyticsUser2025!@127.0.0.1:5436/retailer_analytics

# NextAuth.js Configuration
# Generate secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here
NEXTAUTH_URL=http://localhost:3000

# Optional: Cloud SQL (production)
# SV_CLOUD_SQL_CONNECTION_NAME=project:region:shareview-db
# RSR_CLOUD_SQL_CONNECTION_NAME=project:region:rsr-db

# Optional: Node Environment
# NODE_ENV=development
